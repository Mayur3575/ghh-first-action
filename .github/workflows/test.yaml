name: first workflow
on : workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print greeting
      - run: echo "Hello World!"
      - name: Print goodbye
      - run: echo "Done -Bye!"
      - shell: bash
      
